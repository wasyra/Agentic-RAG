# PostgreSQL
# - App en el host + solo `db` en Docker: usa localhost
DATABASE_URL=postgresql://rag:rag@localhost:5432/rag_kb
# - App en contenedor (`docker compose up web`): Compose inyecta DATABASE_URL con host `db`

# Backend FastAPI (obligatorio en desarrollo híbrido: Next en el host + API en el host).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Las API keys se configuran en la UI (/settings) y viven en localStorage (cabeceras X-AI-Provider, X-API-Key).
